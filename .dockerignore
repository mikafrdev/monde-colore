# dependencies
node_modules
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
coverage

# next.js
.next
out

# production
build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.log

# env files (jamais dans l'image, injectés via docker-compose env_file)
.env*

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma (généré pendant le build Docker lui-même, pas besoin de copier celui du host)
/src/lib/prisma/generated

# uploads (jamais dans l'image, montés via volume Docker)
public/*
!public/.gitkeep

# git et docker lui-même
.git
.gitignore
Dockerfile
.dockerignore
docker-compose.yml

# scripts & bases de données locales
deploy.sh
*.db
*.db.bak

media/
